<Window x:Class="MatchaAccounting.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Matcha Accounting - Login" Height="500" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#222">

    <Window.Resources>
        <!-- 定义样式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧品牌区域 -->
        <Border Grid.Column="0" Background="#1a1a1a">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🍵" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="Matcha Accounting" FontSize="32" FontWeight="Bold" 
                          Foreground="#4CAF50" HorizontalAlignment="Center"/>
                <TextBlock Text="Professional Financial Management" FontSize="16" 
                          Foreground="#A8A8A8" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- 右侧登录表单 -->
        <Border Grid.Column="1" Background="#222" Padding="40">
            <StackPanel VerticalAlignment="Center">
                <!-- 标题 -->
                <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" 
                          Foreground="#E8E8E8" Margin="0,0,0,30" HorizontalAlignment="Center"/>

                <!-- 用户名 -->
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Username" Foreground="#A8A8A8" FontSize="12" 
                              FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtUsername" Style="{StaticResource ModernTextBox}"
                            Height="40"/>
                </StackPanel>

                <!-- 密码 -->
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="Password" Foreground="#A8A8A8" FontSize="12" 
                              FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernPasswordBox}"
                                Height="40"/>
                </StackPanel>

                <!-- 登录按钮 -->
                <Button x:Name="btnLogin" Content="Sign In" Style="{StaticResource ModernButton}"
                        Click="btnLogin_Click" Height="45"/>

                <!-- 错误消息 -->
                <TextBlock x:Name="lblError" Foreground="Red" HorizontalAlignment="Center" 
                          Margin="0,20,0,0" FontSize="12" TextWrapping="Wrap" 
                          Text="Invalid username or password." Visibility="Collapsed"/>

                <!-- 底部链接 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <TextBlock Text="Don't have an account? " Foreground="#A8A8A8" FontSize="12"/>
                    <TextBlock Text="Contact Administrator" Foreground="#4CAF50" FontSize="12"
                              FontWeight="SemiBold" Cursor="Hand">
                        <TextBlock.ToolTip>
                            <ToolTip Content="Please contact your system administrator to create an account"/>
                        </TextBlock.ToolTip>
                    </TextBlock>
                </StackPanel>

                <!-- 默认用户提示 -->
                <Border Background="#2a2a2a" BorderBrush="#4CAF50" BorderThickness="1" 
                        CornerRadius="4" Margin="0,20,0,0" Padding="10,8" Visibility="Collapsed"
                        x:Name="defaultUserHint">
                    <StackPanel>
                        <TextBlock Text="Default Login Credentials" Foreground="#4CAF50" 
                                  FontWeight="Bold" FontSize="11" Margin="0,0,0,4"/>
                        <TextBlock Text="Username: admin" Foreground="#A8A8A8" FontSize="10"/>
                        <TextBlock Text="Use an account issued by the web Owner panel" Foreground="#A8A8A8" FontSize="10"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
